---
import { Image } from 'astro:assets'
import type { ImageMetadata } from 'astro'
import type { HTMLAttributes } from 'astro/types'
import Card from '@/components/card.astro'
import Text from '@/components/text.astro'
import { cn } from '@/utils/style'

interface Props extends HTMLAttributes<'article'> {
  title: string
  description: string
  subtitle: string
  image: {
    src: ImageMetadata
    alt: string
    class: string
  }
}

const {
  title,
  description,
  subtitle,
  image,
  class: className,
  ...delegated
} = Astro.props
---

<Card class={cn("relative overflow-hidden min-h-[430px] h-full", className)} {...delegated}>
    <div class="relative z-10 space-y-4">
        <h3 class="text-2xl font-medium font-pally">
            {title}
        </h3>
        <Text class="font-medium">
            {description}
        </Text>
        <Text class="font-pally text-black/60 font-bold max-w-[250px] text-2xl balance">
            {subtitle}
        </Text>
    </div>
    <Image
        width={200}
        src={image.src}
        alt={image.alt}
        class={cn("absolute bottom-0 right-0", image.class)}
    />
</Card>