---
import type { HTMLAttributes } from 'astro/types'

import { cn } from '../utils/style'

type Props = HTMLAttributes<'div'>
const { class: className, ...delegated } = Astro.props
---

<div class={cn('container mx-auto', className)} {...delegated}>
    <slot />
</div>